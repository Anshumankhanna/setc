#include "utilities.h"

// [[nodiscard]] extern inline size_t max_size_t(const size_t a, const size_t b);
[[nodiscard]] extern inline bool ncmp(const char8_t *const restrict str1,
									  const char8_t *const restrict _str2,
									  const size_t size);
